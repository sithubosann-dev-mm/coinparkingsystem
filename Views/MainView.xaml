<UserControl x:Class="CoinParkingSystem.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:localConv="clr-namespace:CoinParkingSystem.Converters"
             mc:Ignorable="d"
             Width="1024" Height="690"
             Background="#1C1C1C">

    <UserControl.Resources>
        <localConv:SlotSelectionConverter x:Key="SlotSelectionConverter"/>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <Style x:Key="Step1VisibilityStyle" TargetType="FrameworkElement">
            <Setter Property="Visibility" Value="Visible"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsInputtingDetails}" Value="True">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ModernTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#111111"/>
            <Setter Property="Foreground" Value="#FFCC00"/>
            <Setter Property="BorderBrush" Value="#FFCC00"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="FontSize" Value="26"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="4"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Focusable" Value="False"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="90"/>
            <RowDefinition Height="530"/>
            <RowDefinition Height="70"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#111111" BorderBrush="#FFA500" BorderThickness="0,0,0,4">
            <Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="30,0,0,0">
                    <Button Content="◀ 戻る (BACK)" Command="{Binding BackClickCommand}" Visibility="{Binding IsInputtingDetails, Converter={StaticResource BoolToVis}}" Width="130" Height="38" FontSize="14" FontWeight="Bold" Foreground="White" Background="#444444" BorderThickness="0" Margin="0,0,15,0" Cursor="Hand"/>

                    <Border Width="42" Height="42" BorderBrush="#FFA500" BorderThickness="3" CornerRadius="21" Margin="0,0,10,0">
                        <TextBlock Text="P" Foreground="#FFA500" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </StackPanel>

                <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,2,0,2">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,4">
                        <TextBlock Text="☀️ 昼間 (8:00~22:00)：" Foreground="White" FontSize="13" FontWeight="Bold" VerticalAlignment="Center"/>
                        <TextBlock Text="30分 / 200円" Foreground="#FFCC00" FontSize="14" FontWeight="Black" VerticalAlignment="Center"/>

                        <Border BorderBrush="#444444" BorderThickness="1,0,0,0" Margin="15,2,15,2" Height="14"/>

                        <TextBlock Text="🌙 夜間 (22:00~8:00)：" Foreground="White" FontSize="13" FontWeight="Bold" VerticalAlignment="Center"/>
                        <TextBlock Text="60分 / 100円" Foreground="#FFCC00" FontSize="14" FontWeight="Black" VerticalAlignment="Center"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <TextBlock Text="最大料金：" Foreground="White" FontSize="14" FontWeight="Black" VerticalAlignment="Center"/>
                        <TextBlock Text="月～金" Foreground="#888888" FontSize="13" FontWeight="Bold" VerticalAlignment="Center" Margin="2,0,0,0"/>
                        <TextBlock Text="700" Foreground="#FFCC00" FontSize="18" FontWeight="Black" VerticalAlignment="Center" Margin="3,0,0,0"/>
                        <TextBlock Text="円" Foreground="White" FontSize="12" FontWeight="Bold" VerticalAlignment="Bottom" Margin="1,0,0,2"/>

                        <Border BorderBrush="#444444" BorderThickness="1,0,0,0" Margin="15,2,15,2" Height="14"/>

                        <TextBlock Text="土日祝" Foreground="White" FontSize="13" FontWeight="Bold" VerticalAlignment="Center"/>
                        <TextBlock Text="1,200" Foreground="#FFCC00" FontSize="18" FontWeight="Black" VerticalAlignment="Center" Margin="3,0,0,0"/>
                        <TextBlock Text="円" Foreground="White" FontSize="12" FontWeight="Bold" VerticalAlignment="Bottom" Margin="1,0,0,2"/>
                    </StackPanel>
                </StackPanel>

                <Border HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,30,0" 
                        Background="#222222" BorderBrush="#888888" BorderThickness="1" CornerRadius="6" Padding="10,5,10,5">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="📞緊急連絡先：" Foreground="#888888" FontSize="12" FontWeight="Bold" VerticalAlignment="Center"/>
                        <TextBlock Text="XXXX-XXX-XXX" Foreground="#FFA500" FontSize="14" FontWeight="Black" VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="30,10,30,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.1*"/>
                <ColumnDefinition Width="0.9*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#2D2D2D" BorderBrush="#FFCC00" BorderThickness="4" CornerRadius="15" Margin="15" Padding="15">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="PARKING MAP" Foreground="#FFA500" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,15"/>

                    <ItemsControl Grid.Row="1" ItemsSource="{Binding ParkingSlots}" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Rows="3" Columns="5"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Width="65" Height="65" Margin="6" CornerRadius="8" BorderThickness="3.5">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="BorderBrush" Value="#FFCC00"/>
                                            <Setter Property="Background" Value="#202020"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsOccupied}" Value="True">
                                                    <Setter Property="BorderBrush" Value="#FF3B30"/>
                                                    <Setter Property="Background" Value="#3A2222"/>
                                                </DataTrigger>
                                                <DataTrigger Value="True">
                                                    <DataTrigger.Binding>
                                                        <MultiBinding Converter="{StaticResource SlotSelectionConverter}">
                                                            <Binding Path="SlotNumber"/>
                                                            <Binding RelativeSource="{RelativeSource AncestorType=UserControl}" Path="DataContext.TypedSlotNumber"/>
                                                        </MultiBinding>
                                                    </DataTrigger.Binding>
                                                    <Setter Property="BorderBrush" Value="#FF3B30"/>
                                                    <Setter Property="Background" Value="#551A1A"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock Text="{Binding SlotNumber}" Foreground="#FFA500" FontSize="22" FontWeight="Black" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <TextBlock Grid.Row="2" HorizontalAlignment="Center" Margin="0,15,0,0" Foreground="White" FontSize="22" FontWeight="Bold" Text="{Binding AvailableSlotsCount, StringFormat=Available: {0}}"/>
                    <TextBlock Grid.Row="2" HorizontalAlignment="Center" Margin="0,50,0,0" Foreground="White" FontSize="12" FontWeight="Bold" Text="© Copyright TEAM1"/>
                </Grid>
            </Border>

            <Border Grid.Column="1" Background="#2D2D2D" BorderBrush="#FFA500" BorderThickness="4" CornerRadius="15" Margin="15" Padding="20,15,20,15">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="170"/>
                        <RowDefinition Height="210"/>
                        <RowDefinition Height="60"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Style="{StaticResource Step1VisibilityStyle}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="駐車スペース番号入力 (Enter Slot Number)" Foreground="#FFA500" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,15,0,10"/>
                        <TextBox Grid.Row="1" Text="{Binding TypedSlotNumber, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBoxStyle}" Height="50" Width="260" HorizontalAlignment="Center"/>
                    </Grid>

                    <Grid Grid.Row="0" Visibility="{Binding IsInputtingDetails, Converter={StaticResource BoolToVis}}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="28"/>
                            <RowDefinition Height="70"/>
                            <RowDefinition Height="70"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Text="{Binding SelectedSlotGuide}" Foreground="#FF3B30" FontSize="18" FontWeight="Black" HorizontalAlignment="Center" VerticalAlignment="Top"/>

                        <Grid Grid.Row="1" Margin="5,0,5,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Text="車両番号 (Car Plate Number)" Foreground="#FFA500" FontSize="13" FontWeight="Bold" Margin="0,0,0,1"/>
                            <TextBox Grid.Row="1" Text="{Binding FormattedCarPlateNumber, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBoxStyle}" Height="40" VerticalAlignment="Top"/>
                        </Grid>

                        <Grid Grid.Row="2" Margin="5,0,5,0">
                            <Grid.Style>
                                <Style TargetType="Grid">
                                    <Setter Property="Visibility" Value="Visible"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ActionButtonText}" Value="出庫 / 精算">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Grid.Style>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Text="電話番号 (Phone Number)" Foreground="#FFA500" FontSize="13" FontWeight="Bold" Margin="0,0,0,1"/>
                            <TextBox Grid.Row="1" Text="{Binding FormattedPhoneNumber, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBoxStyle}" Height="40" VerticalAlignment="Top"/>
                        </Grid>
                    </Grid>

                    <UniformGrid Grid.Row="1" Rows="4" Columns="3" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,5,0,5">
                        <UniformGrid.Resources>
                            <Style TargetType="Button">
                                <Setter Property="Width" Value="75"/>
                                <Setter Property="Height" Value="44"/>
                                <Setter Property="Margin" Value="5,2,5,2"/>
                                <Setter Property="FontSize" Value="22"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="Background" Value="#444444"/>
                                <Setter Property="Cursor" Value="Hand"/>
                                <Setter Property="Command" Value="{Binding DataContext.NumberClickCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="6">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </UniformGrid.Resources>

                        <Button Content="1" CommandParameter="1"/>
                        <Button Content="2" CommandParameter="2"/>
                        <Button Content="3" CommandParameter="3"/>

                        <Button Content="4" CommandParameter="4"/>
                        <Button Content="5" CommandParameter="5"/>
                        <Button Content="6" CommandParameter="6"/>

                        <Button Content="7" CommandParameter="7"/>
                        <Button Content="8" CommandParameter="8"/>
                        <Button Content="9" CommandParameter="9"/>

                        <Button Content="-" CommandParameter="-"/>
                        <Button Content="0" CommandParameter="0"/>
                        <Button Content="C" Command="{Binding DataContext.ClearClickCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" Background="#842029"/>
                    </UniformGrid>

                    <Button Grid.Row="2" Content="{Binding ActionButtonText}" Command="{Binding MainActionCommand}" Height="50" VerticalAlignment="Bottom" Margin="10,0,10,0" FontSize="18" FontWeight="Bold" Foreground="White" Background="#0F5132" BorderBrush="#FFA500" BorderThickness="2" Cursor="Hand">
                        <Button.Resources>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" CornerRadius="6">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Resources>
                    </Button>
                </Grid>
            </Border>
        </Grid>

        <TextBlock Grid.Row="2" Text="コインパーキング管理システム" Foreground="#888888" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
    </Grid>
</UserControl>